name brainA;
block main {
    changeblock gh;
    $start;
    test 2 else $start;
    inline asdf;
}
block asdf {
    sense ahead food else $start;
    move else $start;
    changeblock gh;
}
block gh {
    turn right;
    changeblock main;
}